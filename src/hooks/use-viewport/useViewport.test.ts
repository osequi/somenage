import { useViewport } from ".";

it("Fails back silently with no arguments", () => {
  /**
   * // NOTE: useViewport() relies on React Responsive hooks, therefore can be called inside a React function component only ...
   */
  //expect(useViewport()).toBeNull();
});
